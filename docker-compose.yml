version: '3.9'

volumes:
  next-js-data:

services:
  app:
    image: ldiiso/nextjs:1.0.0
    ports:
      - target: 3000
        published: 3000
        protocol: tcp
        mode: host
    volumes:
      - next-js-data:/usr/src/app




